apiVersion: v1
kind: Service
metadata:
  name: wealthwise-agentic-ai
  namespace: wealthwise
  labels:
    app.kubernetes.io/name: wealthwise-agentic-ai
spec:
  type: ClusterIP
  ports:
    - port: 5200
      targetPort: 5200
      protocol: TCP
  selector:
    app.kubernetes.io/name: wealthwise-agentic-ai
